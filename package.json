{
    "name": "datahacks-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd ai-bill-simulator && npm install && npm run build && cd .. && cp -r ai-bill-simulator/dist ."
    }
}